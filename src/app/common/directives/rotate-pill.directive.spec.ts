import { RotatePillDirective } from './rotate-pill.directive';

describe('RotatePillDirective', () => {
  it('should create an instance', () => {
    const directive = new RotatePillDirective();
    expect(directive).toBeTruthy();
  });
});
